{
  "name": "dev-needs",
  "version": "1.2.0",
  "description": "Dev AI Skills installer for Claude, Windsurf, Cursor, Copilot and more",
  "type": "module",
  "bin": {
    "dev-needs": "dev-skills-cli/bin/cli.js"
  },
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test":  "npm test --workspaces --if-present",
    "start": "node dev-skills-cli/bin/cli.js"
  },
  "dependencies": {
    "chalk":     "^5.3.0",
    "commander": "^11.1.0",
    "enquirer":  "^2.4.1",
    "ora":       "^7.0.1",
    "boxen":     "^7.1.1",
    "figures":   "^6.0.1",
    "fs-extra":  "^11.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
